/**
 * A simple greeter module to demonstrate TypeScript functionality
 */

/**
 * Generates a greeting message
 * @param name - The name to greet
 * @returns A greeting message
 */
export function greet(name: string): string {
  return `Hello, ${name}!`;
}
